func fatorial(n: int): int {
    if (n <= 1) {
        return 1;
    } else {
        return n * fatorial(n - 1);
    }
}

// Exemplo de chamada da função fatorial
let resultado = fatorial(5);

// Saída: 120
println(resultado);